using System;
using System.Collections.Generic;
using System.Text;
using System.IO;

namespace BrandQQ.Logo.SymbolGlyph
{
    public class SwfGlyph
    {
        /*protected SwfGlyph() { }
        public SwfGlyph(string file)
        {
            if (!File.Exists(file))
            {
                throw new FileNotFoundException("File not found:" + file);
            }

            reader = new SwfReader(file);
        }

        private void Parse()
        {
            Swf swf = reader.ReadSwf();
            
        }

        public void Close()
        {
            if (this.reader != null)
            {
                this.reader.Close();
            }
        }

        private SwfReader reader;
        private GlyphData glyphData;

        #region IGlyph ≥…‘±

        public GlyphData GetData
        {
            get
            {
                return this.glyphData;
            }
        }

        #endregion*/
    }
}
